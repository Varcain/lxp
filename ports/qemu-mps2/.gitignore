# vendored deps (fetched by fetch-deps.sh) + build artifacts — never committed
vendor/
build/
*.elf
*.map
*.log
rootfs_cpio.h
guest/root/
guest/m3root/
guest/*.elf
# guest/rootfs.cpio + guest/rootfs_m3.cpio are PINNED fixtures — intentionally committed
